<h2>Products</h2>

{{#each row in rows}}
	<div class="row">
	{{#each product in row}}
		<div class="col-md-4">
			{{#link-to 'product' product}}
			{{product-list-item name=product.name price=product.price}}
			{{/link-to}}
		</div>
	{{/each}}
	</div>
{{/each}}

